import 'package:flutter/material.dart';

class AddInfoWeather extends StatelessWidget {
  const AddInfoWeather({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}